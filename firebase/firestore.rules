rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: Replace with your production Firestore security rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
